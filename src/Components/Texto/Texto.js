import { Typography } from '@mui/material';
import React from 'react';
const Texto = () => {
    return ( 
        <Typography variant='h3' color={"navy"} textAlign={"center"}>
            No se encontraron tareas, por favor, ingrese una.
        </Typography>
     );
}
export default Texto;
